import { CopyDirectiveDirective } from './copy-directive.directive';

describe('CopyDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CopyDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
